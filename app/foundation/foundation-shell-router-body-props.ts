/**
 * Phase 5.8 — render-scope props for FoundationShellRouterBody.
 * Values are produced by FoundationPageClientInner; typed loosely here to avoid
 * duplicating hundreds of cross-tab derivation types.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type FoundationShellRouterBodyProps = Record<string, any>;
