# qg6cpy — Mutatorpunkte fehlen im Season-Rang

**Gemeldet** 22.08.2026, 10:22, „Spieltag · Arena", Spielstand `swnjlk`, Saison 2, Spieltag 1:

> „Beim Spieltagsranking ist immernoch ein Team 1. mit 6,5 + 0,3 Mutator punkten was korrekt ist!
> ABER im Season rank steht rang 2 weil da nur 6,5 kalkuliert werden. das muss sowohl in der
> Spieltagstabelle als auch in der Season ansicht gefixt werden Mutatorpunkte gehören zu den
> punkten der Disziplin!"

## Befund: an diesem Spielstand ist die Rechnung heute korrekt — kein Eingriff

Die Regel ist bereits umgesetzt, aus den früheren Meldungen `w4eloo`/`re954b`:

- `lib/standings/standings-preview-engine.ts:504` rechnet den Mutator-Bonus in `pointsDelta`.
- `lib/standings/standings-apply-service.ts:384` ruft nach jeder Buchung
  `zieheSaisonstandPunkteNach` auf — die Heilung für Spieltage, die **vor** jenem Fix gebucht
  wurden. Sie schreibt die Team-Summe aus `buildSeasonPointsLedger`, also dieselbe Zahl, die
  Bühne und Spielerprofil zeigen.

**Nachgemessen am Live-Abbild vom 22.08.** (Nachbuchung gegen jeden Spielstand laufen lassen):

| Spielstand | Stand | geänderte Teams |
|---|---|---|
| **`swnjlk`** (der gemeldete) | S2 MD8 | **0** |
| `hwz8fk` | S2 MD10 | 32 |
| `1hf25q` | S2 MD10 | 32 |
| `0kalpx` | S1 MD6 | 32 |
| `h0z7cl` | S1 MD4 | 32 |

Bei `swnjlk` findet die Nachbuchung **nichts mehr zu korrigieren** — der gebuchte Saisonstand
stimmt dort mit dem Ledger überein, Mutatorpunkte inklusive. Die Meldung stammt von **Spieltag 1**;
inzwischen steht der Spielstand bei Spieltag 8, und die Nachbuchung hat bei jeder Buchung
dazwischen geheilt.

Die anderen Spielstände zeigen 32 geänderte Teams — dort *wäre* der Stand falsch, aber sie werden
beim nächsten Buchen von selbst geheilt. Kein Handlungsbedarf.

## Was ich NICHT behaupte

Ich kann den gemeldeten Widerspruch an Spieltag 1 nicht mehr reproduzieren, weil dieser Zustand
im Spielstand nicht mehr existiert. Ob er damals aus der noch nicht gelaufenen Nachbuchung kam
(wahrscheinlich) oder aus einer zweiten, unentdeckten Stelle in der **Anzeige**, lässt sich am
heutigen Abbild nicht mehr unterscheiden.

**Ohne belegte Ursache fasse ich die Rechnung nicht an** — ein Eingriff auf Verdacht in die
Punktebuchung ist die teuerste Sorte Fehler, die hier möglich ist.

**Wenn du den Widerspruch wieder siehst:** melde ihn bitte mit dem Spieltag, an dem er auftritt,
und ob der Spieltag schon gewertet war. Dann ist er in Minuten eingegrenzt.
